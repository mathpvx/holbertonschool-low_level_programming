#include <stdio.h>

/**
 * main - prints alphabet in reverse
 *
 * Return: always 0 (success)
 */

int main(void)
{
	char herve;

	for(herve = 'z'; herve <= 'a'; herve++)
	{
	putchar(herve);
	}
	putchar('\n');

	return (0);
}
