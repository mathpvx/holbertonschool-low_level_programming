#include "main.h"

/**
 * _islower - returns 0 or 1 depending if its lowercase or not
 *
 * @c: the char that is checked
 *
 * Return: 0 or 1
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');

}
