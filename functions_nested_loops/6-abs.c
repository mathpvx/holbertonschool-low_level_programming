#include "main.h"

/**
 * _abs - computes the absolute value of an integer
 *
 * @n: number to compute
 *
 * Return: n if number is positive, -n if number is negative
 */

int _abs(int n)
{
	if (n < 0)
	{
		return (n);
	}
	else
	{
		return (-n);
	}
}
