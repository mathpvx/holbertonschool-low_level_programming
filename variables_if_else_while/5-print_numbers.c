#include <stdio.h>

/**
 * main - prints base 10 numbers
 *
 * Return: always 0 (success)
 */

int main(void)
{
	int bt;
	for (bt = 0; bt < 10; bt++)

	{
	printf("%d", bt);
	}
	putchar('\n');

	return (0);
}
