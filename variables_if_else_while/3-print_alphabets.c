#include <stdio.h>
/**
 * main - prints alphabet in lowercase and uppercase
 *
 * Result: always 0 (success)
 */
int main(void)
{
	char l;

	for (l = 'a'; l <= 'z'; l++)
	putchar('l');

	char U;

	for (U = 'A'; U <= 'Z'; U++)
	putchar('\n');

	return (0);
}
