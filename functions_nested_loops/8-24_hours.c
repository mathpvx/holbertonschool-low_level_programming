#include "main.h"

/**
 *
 */

void jack_bauer(void)
{
	int a;
	int b;
	int c;
	int d;
	int e;
	
	for (a = 0; a <= 2; a++)
	{
		_putchar(a);
	}

	 for (b = 0; b <= 2; b++)
        {
                _putchar(b);
        }
	  for (c = 0; c <= 2; c++)
        {
                _putchar(c);
        }
	   for (d = 0; d <= 2; d++)
        {
                _putchar(d);
        }
	    for (e = 0; e <= 2; e++)
        {
                _putchar(e);
        }
	    _putchar('\n');
}
